x_value
y_value

x_pos = positive(x_value) # convert to positive value if negative
y_pos = positive(y_value)

jal UPDATE_LOOP_FUNCTION  # this is a function that just waits for system update

if (x_pos>300 && y_pos<300):
	if x_value is negative:
		orientation=North
	else if x_value is positive:
		orientation=South
else-if (x_pos<300 && y_pos>300)
	if y_value is negative:
		orientation=East
	else if y_value is positive:
		orientation=West

clear the acknowledge signal    
jal UPDATE_LOOP_FUNCTION		

		
if (x_pos>300 or y_pos>300)
	move_forward


clear the acknowledge signal 